# electron-dropbox-screenshot

A simple Dropbox integrated screenshot uploader. Built with Electron, uses `screencapture` from CLI.  
Actually it works only on Mac.

```
npm install
npm start
```

Default key combination for capturing screen is `Cmnd + Shift + 1`.
